<UserControl x:Class="AppConfigurator.UserControls.ColorPickerUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:WpfToolkit="clr-namespace:Xceed.Wpf.Toolkit;assembly=Xceed.Wpf.Toolkit"
             xmlns:local="clr-namespace:AppConfigurator.UserControls"
             xmlns:converters="clr-namespace:AppConfigurator.ValueConverters"
             mc:Ignorable="d" 
             d:DesignHeight="40" d:DesignWidth="40">
    <Grid>
        <WpfToolkit:ColorPicker Name="ColorPicker" 
                                SelectedColor="{Binding Path=Value}"
                                ShowTabHeaders="False"
                                ColorMode="ColorCanvas"
                                />
    </Grid>
</UserControl>
